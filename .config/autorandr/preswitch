#!/bin/bash

dunstify -i "computer" -t 2000 "Updated Display Output" "Switched to $AUTORANDR_CURRENT_PROFILE profile"

# stop all polybar instances, as a monitor may have been disconnected
killall polybar
killall hudmanager

