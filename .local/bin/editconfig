#!/bin/bash

# Prompt user to open a config directory in their text editor

pushd ~/.config &>/dev/null

IFS=,
list=(*)
choice="${1-$(choose " Edit Config" "" "${list[*]}")}"
[[ -n "$choice" ]] && cd "$choice" && "$TERMINAL" -e "$EDITOR" . &

popd &>/dev/null
