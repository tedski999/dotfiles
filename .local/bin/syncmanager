#!/bin/bash

# Automatically sync directories regularly with Unison

notification_id=$RANDOM
trap "export SYNCMANAGER_MANUAL=true" 10 # USR1

while true; do

	# TODO: sync Documents, Music, Pictures, Videos, .local/backups, .gnupg and passwords

	sleep 600 &
	wait
done
